const Footer = props => {
    return (
        <footer className="App-footer">
            <p>
                This is the base template for the footer
            </p>
        </footer>
    );
}
  
export default Footer;