import NavBar from './NavBar'

const Header = props => {
    return (
        <header className="App-header">
            <NavBar/>
            <p>
                Micah Banschick
            </p>
        </header>
    );
}
  
export default Header;