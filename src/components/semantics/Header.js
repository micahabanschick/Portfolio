import Nav from './Nav'

const Header = props => {
    return (
        <header className="App-header">
        <Nav/>
            <p>
                This is the base template for the header
            </p>
        </header>
    );
}
  
export default Header;