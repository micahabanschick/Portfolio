const Article = props => {
    return (
        <article className={props.colorType}>
            <p>
                This is the base template for the article
            </p>
        </article>
    );
}
  
export default Article;