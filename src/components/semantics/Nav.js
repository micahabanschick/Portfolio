const Nav = props => {
    return (
        <nav className="App-nav">
            <p>
                This is the base template for the nav
            </p>
        </nav>
    );
}
  
export default Nav;